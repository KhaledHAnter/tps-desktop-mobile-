import 'package:flutter/material.dart';

class SingleExerciseStatistics extends StatelessWidget {
  const SingleExerciseStatistics({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SafeArea(
          child: Column(
        children: [],
      )),
    );
  }
}
